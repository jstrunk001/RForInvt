# Naming Inventory & Rename Map

Ground truth = `NAMESPACE` exports (snapshot at audit time). Standard = snake_case.

## Exported functions — casing

Of the exported functions, the overwhelming majority are already snake_case. The outliers requiring
a **breaking rename**:

| Old (exported) | New | Notes |
|---|---|---|
| `sampleSystematic` | `sample_systematic` | camelCase → snake_case; update file `sampleSystematic.R`. |
| `pandermod` | `pander_mod` | + S3 methods `pandermod.list/.lm/.simex` → `pander_mod.*`. |
| `fvs_protype_params` | `fvs_prototype_params` | **misspelling** ("protype"); rename file + fn + call sites. |

Kept as-is (acronym prefix, already conventional): `NVEL_volume`, `NVEL_buck`, `NVEL_merch`,
`NVEL_voleq`, `NVEL_biomass`, `NVEL_wtfactor`. Short domain helpers kept: `tpa`, `tph`, `ba_ft`,
`dbcl`, `dbcl_y`, `spp_y`, `dbcl_spp_y`, `bs`, `fs`, `nms_vec`.

## Argument renames (camelCase → snake_case)

Applied consistently within each family; record exact per-function lists as edited.

| Family | Old args | New args |
|---|---|---|
| NVEL (`volume/buck/voleq/biomass/wtfactor`) | `dbhNm`, `htNm`, `spcdNm`, `voleqNm`, `htPrd1Nm`, `regionNm`, `forestNm`, `districtNm`, `dll_func*` | `dbh_nm`, `ht_nm`, `spcd_nm`, `voleq_nm`, `ht_prd1_nm`, `region_nm`, `forest_nm`, `district_nm`, `dll_func_*` |
| knn (`yai_id`, `newtargets_id`, `yai_weights`, `tl_impute*`, `yai_cv`) | `xNms`, `yNms`, `idNm`, `dtype`, `omity`, `yaimod` | `x_nms`, `y_nms`, `id_nm`, `dtype` (kept; yaImpute term), `omit_y`, `yai_mod` |
| `yai_r2` internals | `vi`, `vo`, `notFound`, `R2_in` | `v_pred`, `v_obs`, `not_found`, `r2_in` |

## Internal-variable cleanup (no API impact)

- `estimators.r` — mixed `respNm`/`resp_nm`, `v_t_rs`, `mn_rs` → consistent snake_case.
- `ols_modeling.R` — mixed `nrow`/`n_boot`, `bic_id` → snake_case; fix `modk`→`mod_k`.
- `knn_tools.R` — camelCase locals (`wts`, `cols_knn_id`) → snake_case where camelCase.

## Downstream call sites to update on rename

- `NAMESPACE` (regenerated by `devtools::document()`).
- `man/*.Rd` (regenerated).
- `tests/testthat/*` referencing renamed functions/args.
- `inst/examples/FVS_Example.Rmd` (uses `fvs_*` family), `README.md`.
- Cross-calls inside the package (e.g. NVEL functions calling each other; `compile_*` calling helpers).
