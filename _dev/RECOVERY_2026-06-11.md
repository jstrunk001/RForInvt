# Recovery state — package review fix re-application (2026-06-11)

## What happened
During the fix-implementation session, a concurrent git history rewrite in this repo
(`filter-branch` + `commit --amend` + `reset: moving to HEAD`, visible in reflog) committed
the FIRST batch of my edits into HEAD `10368e5 "Add fia_data accessors and fix NVEL volume/merch
internals"`, then the final `reset` DISCARDED the SECOND batch of uncommitted edits. My fixes were
never separately committed, so the discarded ones are not git-recoverable and are being re-applied
by hand from the session record.

## SURVIVED (already committed in HEAD 10368e5 — do NOT re-apply)
NVEL_volume, NVEL_buck, NVEL_merch, NVEL_voleq, NVEL_wtfactor, nvel_internal,
compile_plots, compile_trees, ols_modeling, pander_mod, sample_systematic, yai_r2,
tests/test-compile_trees.R, tests/test-nvel_volume.R, _dev/plan_package_review_2026-06.md.

Verified signatures present: NVEL region=NA, yai_r2 residual-variance R2, sample_systematic
`idx<=N`, reg_model rank `c("rsq","adjr2")`, ba_ft 0.005454154, `.formatTL2NVEL` removed,
compile_plots "FILTERED tables", ols formula sandbox/leading-response, pander "Resid. Std. Error".

## RE-APPLIED this session after the reset (done)
- DESCRIPTION: added `jsonlite` to Imports.
- R/dist_vec.R: vectorized (storage.mode double, sweep/rowSums), @return filled, if(F) block removed.

## RE-APPLICATION COMPLETE (2026-06-11, second pass)
All 15 files below were re-applied and the FULL suite passes:
`failed: 0  warnings: 41  skipped: 1  passed: 494` (NOT_CRAN=true). Also fixed
fvs_prototype_keyfile.R (@seealso fvs_load + \dontrun example). These changes are
UNCOMMITTED working-tree edits — commit them (ideally on a branch) to protect against
another reset/filter-branch.

Still NOT done (genuinely new work, deferred): aggregate2.R (qualify stats::aggregate +
remove if(F)), file_stamp.R (examples pass stamp as path; doc tempdir; qualify utils::timestamp),
Phase 0 regression tests, roxygenize, R CMD check.

## (ORIGINAL CHECKLIST — now re-applied) STILL TO RE-APPLY (lost by reset)
Re-apply exactly the edits described in `_dev/plan_package_review_2026-06.md` for each:

1. R/estimators.r — `.rand` bs branch: equal-prob resample + weight rescale (drop double-weight);
   `.summary` rmse_srs = `res0$se_m*sqrt(res$n)`; `.pop_test` `pi`->`p_i`; `.str` `inherits(pop,...)`,
   `has_more=which(ni>=2)`, `which.min(abs(has_one[i]-has_more))`; `.pop_test` s_ix `inherits(...,"try-error")`;
   add HH/with-replacement (no-FPC) comment on `.rand`.
2. R/make_strata.R — restore `#' @export`/`#' @rdname make_strata` on assign_strata (was `##'`);
   eq breaks `[-c(1,length(seq))]` for x1, nested-x2, non-nested-x2; non-nested x2 use n2 not n1 +
   upper sentinel `r_x2[2]+dr_x2`; collapse `idx_good[which.min(...)]`; NA-drop of x1/x2 up front;
   "Cannot nest x2" typo; remove trailing if(F) dev block.
3. R/knn_tools.R — impute_id tolerant tag (update_id||updateID); yai_weights `dtype="eq"` =
   `dist_in*0 + 1/ncol(dist_in)`; newtargets_id ref/target id-collision warning; yai_id omity
   `data[-omity,]`; `\\.o$` regex; yai_cv true weighted RMSE (wmse incl. bias).
4. R/model_predict.R — `.mp_safe_env()` sandbox + use in `.fn_predict`; constant-formula
   `rep(res,nrow(dat))`; standardize transform reads spec center/scale; `response` NULL guard;
   suffix_predict default NULL + doc.
5. R/fia_clean_best_cds.R — `"filter_cds"` step name (was filter_pts); `%in% "HPGPS"`/`"GPS"`;
   composite plot_key dedupe; drop `| TRUE`; PLOT POINT guarded + `"PLOT POLYGON"` key;
   `sf::st_cast`; remove if(F) block.
6. R/search_text.R — default pattern `"\\.[rR]$"`; clusterMap `file=files` + MoreArgs.
7. R/fvs_run.R — compute `unq_db=unique(key_df$output_db)` at top (drop noquote); shQuote
   fvs_path+keyfile; parallel `split(fvs_runs,key_df$output_db)` serial-per-group; unconditional
   `dbDisconnect(con_dbi)` per iter; warn on overwrite unlink; @return + @seealso doc.
8. R/fvs_make_keyfiles.R — `id="std_id"` default; remove `create_db`; `"\\.key$"`/`"\\.out$"`;
   key_nms literal `paste0(prefix,id_value,suffix)`; @return/@param/@seealso doc.
9. R/fvs_load.R — `zips=list.files(dir_rfvs,pattern="\\.zip$")` (drop sapply); guard max_zip;
   @seealso doc.
10. R/fvs_prototype_params.R — fix description copy-paste; example std_id + \dontrun wrap.
11. R/file_version.R — flock lock around update_paths+tracking_get; `.update_paths` conditional
    write + suppressWarnings normalizePath mustWork=FALSE; drop dead last_id line in purge;
    inherits(err); @seealso file_stamp; remove trailing if(T) dev block.
12. R/archive_table.R — move file_version + (xlsx) .prepare_for_xlsx + datetime-style INSIDE
    .safe_write for all 4 writers (path=NULL init); `.normalize_colnames` second-order-collision
    rewrite; rename loop var `c`->`cc` (done within xlsx rewrite).
13. R/model_archive_save.R — checked atomic rename for predictor-spec JSON; warn when vcov supplied
    without system_id; model_id uniqueness compare as.character both sides.
14. R/fia_make_geom.R — add `crs=NA` param + pass to st_as_sf + @param; offx/offy name validation;
    drop SUBPLOT col from PLOT data_in; @seealso fix; remove if(F) block.
15. R/helper_functions.R — platform guards (write_clipboard warns off-Windows; reslash stops if
    no x off-Windows via get(...,asNamespace("utils"))); `length(x)==1 && is.na(x)`; shared
    @name/@rdname helper_functions; example nms_vec runnable + bs()/fs() in \dontrun.

## Also pending (Phase 0/4, not yet started)
- fvs_prototype_keyfile.R @seealso `fvs_load_rfvs` -> `fvs_load`.
- file_stamp.R: examples pass stamp as `path`; doc tempfile->tempdir; qualify utils::timestamp.
- aggregate2.R: qualify stats::aggregate; remove trailing if(F) block.
- Phase 0 value-pinning + branch regression tests (see plan §Phase 0).
- Final: roxygenize, full `test_dir` (set NOT_CRAN=true to exercise archive_table integration),
  R CMD check.

## Verification harness
`Rscript C:\temp\rforinvt_runtests.R "<filter-regex>"` runs `devtools::load_all` + `test_dir`.
Baseline before any fixes: 442 passed / 0 failed / 8 skipped. NVEL DLL + FVSie exe ARE available
on this machine, so NVEL and the FVS integration test run for real.
