% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NVEL_buck.R
\name{NVEL_buck}
\alias{NVEL_buck}
\title{Predict Log-Level Volume and Dimensions using NVEL}
\usage{
NVEL_buck(
  dfTL = data.table::data.table(dbh = 5, ht = 5),
  voleq = NA,
  region = 1,
  forest = NA,
  district = NA,
  voleqNm = "voleq",
  regionNm = "region",
  forestNm = "forest",
  districtNm = "district",
  spcdNm = "spcd",
  dbhNm = "dbh",
  htNm = "ht",
  pulpDbNm = "pulpDb",
  sawDbNm = "sawDb",
  htPrd1Nm = "htPrd1",
  htPrd2Nm = "htPrd2",
  upHt1Nm = "upHt1",
  upDb1Nm = "upDb1",
  stumpHtNm = "stumpHt",
  fclassNm = "fclass",
  dbtbhNm = "dbtbh",
  btrNm = "btr",
  vol2biomass = TRUE,
  dll_64 = system.file("lib/VolLibDll20231106/vollib-64bits/vollib.dll", package =
    "RForInvt"),
  dll_32 = system.file("lib/VolLibDll20231106/vollib-32bits/vollib.dll", package =
    "RForInvt"),
  load_dll = TRUE,
  dll_func_vol = "vollib2_r",
  ncore = 1
)
}
\arguments{
\item{dfTL}{data.frame or data.table. Tree list containing DBH, Height, and Species codes.}

\item{voleq}{character. Optional. A specific NVEL volume equation (e.g., "F0162521") to force for all trees.}

\item{region}{integer. USFS Region (1-10). Default 1.}

\item{forest}{character. USFS Forest code (e.g., "01").}

\item{district}{character. USFS District code (e.g., "01").}

\item{voleqNm}{character. Column name in \code{dfTL} containing volume equations.}

\item{regionNm}{character. Column name in \code{dfTL} for USFS Region.}

\item{forestNm}{character. Column name in \code{dfTL} for USFS Forest.}

\item{districtNm}{character. Column name in \code{dfTL} for USFS District.}

\item{spcdNm}{character. Column name in \code{dfTL} for USFS Species Code.}

\item{dbhNm}{character. Column name in \code{dfTL} for DBH (inches).}

\item{htNm}{character. Column name in \code{dfTL} for Total Height (feet).}

\item{pulpDbNm}{character. Optional. Column name for pulpwood top diameter limit.}

\item{sawDbNm}{character. Optional. Column name for sawtimber top diameter limit.}

\item{htPrd1Nm}{character. Optional. Column name for height to primary product top.}

\item{htPrd2Nm}{character. Optional. Column name for height to secondary product top.}

\item{upHt1Nm}{character. Optional. Column name for upper stem height.}

\item{upDb1Nm}{character. Optional. Column name for upper stem diameter.}

\item{stumpHtNm}{character. Optional. Column name for stump height.}

\item{fclassNm}{character. Optional. Column name for Girard Form Class.}

\item{dbtbhNm}{character. Optional. Column name for double bark thickness.}

\item{btrNm}{character. Optional. Column name for bark thickness ratio.}

\item{vol2biomass}{logical. If TRUE, joins with \code{NVEL_wtfactor} to compute green/dry biomass.}

\item{dll_64}{character. Path to the 64-bit vollib.dll.}

\item{dll_32}{character. Path to the 32-bit vollib.dll.}

\item{load_dll}{logical. Whether to load the DLL into the R session.}

\item{dll_func_vol}{character. Entry point name in the DLL. Default "vollib2_r".}

\item{ncore}{integer. Number of cores for parallel processing. Default 1.}
}
\value{
data.table in long format (one row per log).
}
\description{
A high-performance wrapper for the National Volume Estimator Library (NVEL) \code{vollib2_r} entry point.
This function predicts individual log dimensions (length, diameters, volumes) and whole-tree
attributes (total volumes, biomass). It supports multi-core parallel processing for large datasets
and forces internal use of \code{data.table} for memory efficiency.
}
\details{
The function returns a "long-format" table. If a tree has 5 logs, it will result in 5 rows in the
output, each sharing the same tree-level attributes but having unique log-level attributes.
\itemize{
  \item \code{LG_}: Columns prefixed with LG represent log-specific data (e.g., \code{LG_DIB_SMALL}).
  \item \code{TR_}: Columns prefixed with TR represent tree-level totals (e.g., \code{TR_TCFV_ALL}).
}

Note: Parallel execution (\code{ncore > 1}) uses a PSOCK cluster to ensure the non-thread-safe
Fortran DLL is isolated within separate R processes.
}
\examples{


library(data.table)
library(RForInvt)

# --- Example 1: Basic Single-Core Usage ---
df_trees <- data.table(
  tree_id = 1:3,
  spcd = c(202, 202, 122),
  dbh = c(12.5, 24.2, 8.1),
  ht = c(85, 130, 45),
  region = 6,
  forest = "01",
  district = "01"
)

res <- NVEL_buck(
  dfTL = df_trees,
  spcdNm = "spcd",
  dbhNm = "dbh",
  htNm = "ht",
  vol2biomass = TRUE,
  ncore = 1
)

# View logs for the large tree (tree_id 2)
print(res[tree_id == 2, .(tree_id, LG_NUM, LG_LEN, LG_DIB_SMALL, TR_TCFV_ALL)])

# --- Example 2: Parallel Processing for Large Datasets ---
# Create a fake dataset of 10,000 trees
big_trees <- data.table(
  spcd = sample(c(202, 122, 15), 10000, replace = TRUE),
  dbh = runif(10000, 5, 30),
  ht = runif(10000, 30, 150),
  region = 6,
  forest = "01",
  district = "01"
)

# Run in parallel on 4 cores
res_para <- NVEL_buck(
  dfTL = big_trees,
  ncore = 4,
  vol2biomass = FALSE
)

# --- Example 3: Forcing a Specific Volume Equation ---
# Use a specific equation code from the NVEL library (must be valid for the
# trees' region/species, or vollib2_r returns zero volume).
res_forced <- NVEL_buck(
  dfTL = df_trees,
  voleq = "I00FW2W202"
)


}
\author{
Jacob Strunk <someone@somewhere.com>
}
